{
  "deploy": {
    "startCommand": "cd server && pnpm run start",
    "buildCommand": "cd client && npx expo export --platform web && cp -r dist ../server/public && cd ../server && pnpm run build",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
